built-in sort function

qsort( array, array_size, unit_size, compare_function )
sorts anything, doesnt know what the array consists of

compare_function should take const void *

array of pointers to pairs
struct pair *a = *(struct pair **)x

data structure

input
check if a file already exists
fstat
build your own path

readdir make a copy of filename 
